# ── Worker secrets (packages/worker/.dev.vars) ──
BRIDGE_API_TOKEN=
ANTHROPIC_API_KEY=
RESEND_API_KEY=
CONFIG_API_KEY=

# ── Worker vars (packages/worker/wrangler.toml) ──
EMAIL_FROM=scout@yourdomain.com
MOCK_MODE=false

# ── Web app (packages/web/.env) ──
VITE_WORKER_API_URL=http://localhost:8787
VITE_CONFIG_API_KEY=
